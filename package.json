{
  "name": "pi-laya-guardrails",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "keywords": ["pi-package", "laya", "guardrails"],
  "scripts": {
    "test": "node --test tests/*.test.ts && .venv/bin/python -m unittest discover -s tests -p 'test_*.py'"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": ["./extensions/laya-guardrails.ts"]
  }
}
